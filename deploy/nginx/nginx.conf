events {}

http {
  server {
    listen 80;
    server_name _;

    # Replace with actual API/UI upstream once web endpoint is exposed.
    location / {
      return 200 "JIVAN reverse proxy online\n";
      add_header Content-Type text/plain;
    }
  }
}
